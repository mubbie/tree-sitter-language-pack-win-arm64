<!-- Generated by scripts/generate_readme.py. DO NOT EDIT. -->

# tree-sitter-language-pack

{% include 'partials/badges.html.jinja' %}

<div align="center">
  <img width="3384" height="573" alt="Banner" src="{{ project.banner_url }}" />
</div>

<div align="center">
  <a href="{{ project.discord }}">
    <img height="22" src="https://img.shields.io/badge/Discord-Join%20our%20community-7289da?logo=discord&logoColor=white" alt="Discord">
  </a>
</div>

{{ project.description }}

## Overview

**tree-sitter-language-pack** bundles {{ project.language_count }}+ tree-sitter language parsers into a single package with native bindings for multiple programming languages. Ship syntax analysis in your application without managing individual parser dependencies.

## Architecture

```text
tree-sitter-language-pack/
├── crates/
│   ├── ts-pack-core/       # Rust core library
│   ├── ts-pack-python/     # Python (maturin/PyO3) bindings
│   ├── ts-pack-node/       # Node.js (NAPI-RS) bindings
│   ├── ts-pack-java/       # Java (Panama FFI) bindings
│   ├── ts-pack-elixir/     # Elixir (Rustler NIF) bindings
│   ├── ts-pack-ffi/        # C-compatible FFI library
│   └── ts-pack-cli/        # CLI tool
├── packages/
│   └── go/v1/              # Go (cgo) bindings
├── grammars/               # Tree-sitter grammar sources
└── scripts/                # Build and maintenance scripts
```

## Quick Start

{%- set pm_lang_map = {"maven": "xml", "gradle": "groovy", "mix": "elixir", "cargo": "toml", "npm": "sh", "pnpm": "sh", "yarn": "sh", "pip": "sh", "uv": "sh", "go": "sh"} -%}
{% for lang_code, lang in languages.items() %}
### {{ lang.name }}

{% if lang.install_commands is defined -%}
{% for pm, cmd in lang.install_commands.items() -%}
{% if cmd is string and '\n' not in cmd -%}
```sh
{{ cmd }}
```
{% elif cmd is string -%}
```{{ pm_lang_map.get(pm, "text") }}
{{ cmd.strip() }}
```
{% endif -%}
{% endfor -%}
{% elif lang.install_command is defined -%}
{% if lang.install_command is string and '\n' not in lang.install_command -%}
```sh
{{ lang.install_command }}
```
{% else -%}
{%- set first_pm = lang.package_manager[0] if lang.package_manager else "text" -%}
```{{ pm_lang_map.get(first_pm, "text") }}
{{ lang.install_command.strip() }}
```
{% endif -%}
{% elif lang.package_manager | length == 0 -%}
Build from source as part of this workspace.

{% endif -%}

See [{{ lang.name }} README]({{ lang.output_path }}) for full documentation.

{% endfor %}

## Core API

All bindings expose a unified `process()` function for extracting structured intelligence from source code:

| Language | Function |
| -------- | -------- |
| Rust | `ts_pack_core::process(source, &config)` |
| Python | `process(source, ProcessConfig(...))` |
| Node.js | `process(source, { language: '...' })` |
| Go | `registry.Process(source, config)` |
| Java | `registry.process(source, configJson)` |
| Ruby | `TreeSitterLanguagePack.process(source, configJson)` |
| Elixir | `TreeSitterLanguagePack.process(source, configJson)` |
| WASM | `process(source, { language: '...' })` |
| C FFI | `ts_pack_process(registry, source, len, configJson)` |

The `process()` function returns structured analysis including functions, classes, imports, comments, and optionally chunked source segments.

## Features

{% include 'partials/features.md.jinja' %}

## Supported Languages

This pack includes {{ project.language_count }}+ languages. See the [Rust core Cargo.toml](crates/ts-pack-core/Cargo.toml) for the full list of available language features.

## Package READMEs

{% for lang_code, lang in languages.items() -%}
- [{{ lang.name }}]({{ lang.output_path }}) -- {{ lang.description.strip() }}
{% endfor %}

## Contributing

Contributions are welcome! See [CONTRIBUTING.md](CONTRIBUTING.md) for guidelines.

Join our [Discord community]({{ project.discord }}) for questions and discussion.

## License

{{ project.license }} -- see [LICENSE](LICENSE) for details.
